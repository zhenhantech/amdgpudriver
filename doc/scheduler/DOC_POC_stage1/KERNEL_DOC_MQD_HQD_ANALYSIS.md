# Linux Kernel 官方文档：MQD & HQD 深度分析

**来源**: [AMD GPU Driver Core Documentation](https://docs.kernel.org/gpu/amdgpu/driver-core.html)  
**日期**: 2026-02-03  
**重要性**: ⭐⭐⭐⭐⭐ (官方权威文档)

---

## 📖 官方文档关键章节

### GFX, Compute, and SDMA Overall Behavior

这一章节详细解释了 Queue 的工作机制，是理解 MQD/HQD 的核心。

---

## 🏗️ 核心架构概念

### 1. Pipes 和 Queues 的关系

```
┌─────────────────────────────────────────────┐
│                                             │
│  ┌─────────┐         ┌──────────────────┐  │
│  │  Pipe   │ ◄────── │  Multiple Queues │  │
│  │         │         │                  │  │
│  │ (执行器)│         │  Queue 0         │  │
│  │         │         │  Queue 1         │  │
│  │ 一次只  │         │  Queue 2         │  │
│  │ 执行一个│         │  ...             │  │
│  │ Queue   │         │                  │  │
│  └─────────┘         └──────────────────┘  │
│                                             │
└─────────────────────────────────────────────┘
```

**官方描述**:
> Queues must be associated with a Pipe and vice-versa. Every specific hardware IP may have a different number of Pipes and, in turn, a different number of Queues.

**关键点**:
1. ✅ **Queues 必须与 Pipe 关联**
2. ✅ **Pipe 一次只能执行一个 Queue**
3. ✅ **不同硬件 IP 有不同数量的 Pipes 和 Queues**

**示例 (GFX 11)**:
```
GFX 11 前端:
  - 2 个 Pipes
  - 每个 Pipe 有 2 个 Queues
```

---

### 2. Pipe 的工作机制

**官方描述**:
> Pipe is the hardware that processes the instructions available in the Queues; in other words, it is a thread executing the operations inserted in the Queue.

**Pipe 的特性**:
- 🔧 **硬件处理器**: 执行 Queue 中的指令
- 🔄 **Queue 切换能力**: 可以在硬件层面切换 Queue
- ⚡ **只执行 Mapped Queues**: 只处理已映射的队列

**Queue 切换的触发条件**:
1. **Command Stream** (命令流)
2. **Packet by Packet** (逐包)
3. **其他硬件请求** (例如 MES)

---

## 🎯 MQD (Memory Queue Descriptor)

### 官方定义

**原文**:
> Associated with the HQD concept, we have the Memory Queue Descriptor (MQD), which is responsible for storing information about the state of each of the available Queues in the memory.

### MQD 的职责

```
MQD (内存中)
  ├─ 存储 Queue 状态信息
  │   ├─ GPU 虚拟地址
  │   ├─ Save areas
  │   ├─ Doorbell
  │   └─ 其他状态
  │
  └─ 存储 HQD 寄存器
      └─ 用于激活/停用 Queue
```

**官方描述**:
> The state of a Queue contains information such as the GPU virtual address of the queue itself, save areas, doorbell, etc. The MQD also stores the HQD registers, which are vital for activating or deactivating a given Queue.

### MQD 包含的信息

| 内容 | 说明 |
|------|------|
| **GPU Virtual Address** | Queue 在 GPU 地址空间中的位置 |
| **Save Areas** | 保存 Queue 上下文的内存区域 |
| **Doorbell** | 用户态通知硬件的 MMIO 寄存器 |
| **HQD Registers** | 硬件队列描述符寄存器副本 |
| **Queue State** | Queue 的运行状态 |

---

## 🔩 HQD (Hardware Queue Descriptor)

### 官方定义

**原文**:
> Queues within Pipes are defined by the Hardware Queue Descriptors (HQD).

### HQD 的特性

```
HQD (硬件寄存器)
  ├─ 定义 Pipe 内的 Queue
  ├─ HQD_ACTIVE 位
  │   ├─ 1 = Queue 活跃
  │   └─ 0 = Queue 非活跃
  │
  └─ 控制 Queue 的硬件状态
```

**关键寄存器**:
- **HQD_ACTIVE**: 指示 Queue 是否活跃的关键位

**官方描述**:
> The firmware waits for the HQD_ACTIVE bit to change to low before saving the state into the MQD.

---

## 🔄 Queue 切换机制

### 固件的角色

**官方描述**:
> The scheduling firmware (e.g., MES) is responsible for loading HQDs from MQDs and vice versa.

**MES (Micro-Engine Scheduler)**:
- GFX 11+ 的调度固件
- 负责 Queue 的调度和切换
- 替代了旧的 KIQ 机制

---

### Queue 切换流程

#### 1. 停用 Queue (Deactivate)

```
┌─────────────────────────────────────────────┐
│ 1. 固件请求抢占/卸载 Queue                    │
│    firmware requests preemption/unmapping   │
└────────────┬────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────┐
│ 2. 固件等待 HQD_ACTIVE 位变为 LOW            │
│    firmware waits for HQD_ACTIVE = 0        │
└────────────┬────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────┐
│ 3. 将 HQD 状态保存到 MQD                     │
│    save HQD state into MQD                  │
└─────────────────────────────────────────────┘
```

**官方原文**:
> The firmware waits for the HQD_ACTIVE bit to change to low before saving the state into the MQD.

---

#### 2. 激活 Queue (Activate)

```
┌─────────────────────────────────────────────┐
│ 1. 固件将 MQD 状态复制到 HQD 寄存器           │
│    firmware copies MQD state into HQD regs  │
└────────────┬────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────┐
│ 2. 加载任何额外状态                          │
│    load any additional state                │
└────────────┬────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────┐
│ 3. 设置 HQD_ACTIVE 位为 HIGH                 │
│    set HQD_ACTIVE = 1                       │
└────────────┬────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────┐
│ 4. Pipe 开始执行该 Queue 的工作              │
│    Pipe executes work from active Queue     │
└─────────────────────────────────────────────┘
```

**官方原文**:
> To make a different Queue become active, the firmware copies the MQD state into the HQD registers and loads any additional state. Finally, it sets the HQD_ACTIVE bit to high to indicate that the queue is active. The Pipe will then execute work from active Queues.

---

## ❓常见问题澄清：调度策略与切换类型

### Q1: 同一个 Pipe 上存在多个 **active** 的 HQD，运行时是否一定 round-robin？

**结论**：不一定是严格 round-robin。  
官方文档只说明 **Pipe 一次只执行一个 Queue**，并且切换可由不同机制触发（命令流、逐包、MES 等），**并没有规定必须是 round-robin**。实际调度会受以下因素影响：

- **优先级与策略**（runlist/firmware 调度策略）
- **队列是否 mapped**
- **固件（MES/KIQ）行为**
- **硬件实现细节**

> 文档只定义“Pipe 一次只执行一个 Queue”，并未承诺 round-robin 调度策略。  
> 参考: https://docs.kernel.org/gpu/amdgpu/driver-core.html

### Q2: Preemption 导致的 queue switching 和 Unmapping 导致的 queue switching 是否不同？

**结论**：是的，语义不同。  
两者都会涉及 **等待 HQD_ACTIVE=0** 并进行 **HQD → MQD 状态保存**，但目的和后续状态不同：

#### ✅ Preemption（抢占）
- 目标：**短暂停止当前队列，切换到更高优先级队列**
- 通常触发 **CWSR（Wave Save/Restore）**
- 保存当前 wave/context 到 **上下文保存区 + MQD**
- 队列仍处于 **可恢复的 mapped 状态**
- 之后可能 **快速恢复执行**

#### ✅ Unmapping（卸载）
- 目标：**把队列从硬件 runlist 中移除**
- 执行 **unmap_queues_cpsch / remove_queue_mes**
- 队列进入 **inactive/unmapped** 状态
- 恢复执行需要 **重新 map / load MQD → HQD**

#### 关键区别
| 维度 | Preemption | Unmapping |
|------|------------|-----------|
| 目的 | 临时抢占 | 从硬件移除 |
| 状态保存 | HQD → MQD + CWSR | HQD → MQD |
| 是否还在 runlist | 通常仍在 | 否 |
| 恢复成本 | 低 | 较高（需要重新 map） |

> 文档描述中，“firmware requests preemption or unmapping” 使用同一段机制：等待 HQD_ACTIVE=0 并保存 HQD → MQD，但 **unmapping 的语义更强**（从硬件移除），**preemption 更像短期上下文切换**。  
> 参考: https://docs.kernel.org/gpu/amdgpu/driver-core.html

### 补充：KFD 中的 runlist（澄清“runlist”概念）

**说明**：KFD 中确实有 **runlist** 概念。  
它不是纯软件或纯硬件概念，而是 **“软件构建 + 硬件消费”** 的接口机制：

- **软件侧**：KFD/固件构建 runlist（例如 PM4 runlist IB）
- **硬件侧**：MES/CP 消费 runlist，将 MQD 加载到 HQD

因此更严谨的表述是：  
**“queue 必须先被 KFD/固件 map 到调度可见的 runlist，并完成 HQD 加载，才可能出现 HQD_ACTIVE=1 的 active 状态。”**

参考: https://docs.kernel.org/gpu/amdgpu/driver-core.html

---

### runlist 与 MQD 的关系（更细化）

**一句话**：  
**MQD 是“每个 Queue 的状态快照”**，而 **runlist 是“当前调度周期内要执行的队列清单”**。  
runlist 的元素本质上是 **“队列的调度条目/描述”**，这些条目会引用或携带 MQD 中的配置，从而把 MQD 内容加载到 HQD。

#### 1) MQD 是 per-queue 的内存结构
- 存在于系统内存
- 包含队列状态（地址、doorbell、寄存器镜像等）
- 由固件在 **map/activate** 时加载到 HQD

#### 2) runlist 是 per-schedule 的队列列表
- 由 KFD/固件构建（例如 PM4 runlist IB）
- 表示“当前应该被调度的队列集合”
- **每个元素对应一个 Queue**（即一个 MQD 所代表的队列）
- 硬件调度器（MES/CP）读取 runlist，完成 HQD 装载与切换

#### 3) 元素是什么？（概念级理解）
- 可以理解为 **“队列描述条目”**
- 至少包含：Queue ID/类型/优先级/门铃/指向 MQD 的关联信息
- 具体字段取决于硬件/固件实现，但核心是 **“把 MQD 所代表的队列加入调度集合”**

#### 4) 简化关系图

```
Queue (逻辑)
  └─ MQD (内存中的状态快照)
         ↑
         |  runlist 元素指向 / 关联
         ↓
runlist (本轮调度的队列清单)
  └─ MES/CP 读取 runlist → 加载 MQD → HQD_ACTIVE=1
```

> 官方文档只明确了 MQD ↔ HQD 的加载关系以及“mapped queues 才会执行”，  
> runlist 的具体条目格式属于硬件/固件实现细节，但从架构上可以认为 **runlist 的元素是队列调度条目，最终关联到 MQD**。  
> 参考: https://docs.kernel.org/gpu/amdgpu/driver-core.html

---

### Doorbell vs Runlist（提交路径澄清）

**结论**：  
**doorbell 是用户态提交命令的机制**，而 **runlist 是内核/固件调度队列集合的机制**。  
二者是不同层级的“入口”。

#### ✅ 前提关系
```
Queue 被 map → HQD 加载 → HQD_ACTIVE=1
    ↓
用户态 doorbell 写入才会生效
```

#### 简单流程图（调度 vs 提交）

```
用户态提交路径:
  User Space
    └─ Doorbell (MMIO写) → Queue Ring Buffer
          ↑ 需要 HQD_ACTIVE=1

内核调度路径:
  KFD / Firmware
    └─ runlist (调度集合)
          └─ map/unmap → MQD ↔ HQD
```

**一句话**：  
**runlist 决定“哪些队列能跑”，doorbell 决定“队列里有新命令”。**

---

## 🔑 关键机制：HQD_ACTIVE 位

### HQD_ACTIVE 的作用

```
HQD_ACTIVE 寄存器
  │
  ├─ bit[0] = 1  →  Queue 活跃，正在被 Pipe 执行
  │
  └─ bit[0] = 0  →  Queue 非活跃，可以安全操作
```

**这与我们之前的发现完全一致！**

回顾我们的文档：
```markdown
private_github/amdgpudriver/doc/kernel_submit/test_queue_limits/logs_KCQ_config/PRECISE_HQD_COUNTING_METHOD.md

mmCP_HQD_ACTIVE (0x1247):
  - Index: 2 (在 HQD dump 中的位置)
  - bit[0]: 1 = active, 0 = inactive
```

---

## 🎮 KIQ vs MES

### KIQ (Kernel Interface Queue)

**官方定义**:
> This is a control queue used by the kernel driver to manage other gfx and compute queues on the GFX/compute engine. You can use it to map/unmap additional queues, etc.

**用途**:
- 内核驱动的控制队列
- 管理其他 GFX 和 compute queues
- 可以 map/unmap 队列

**注意**:
> This is replaced by MES on GFX 11 and newer hardware.

---

### MES (Micro-Engine Scheduler)

**在 GFX 11+ 上**:
- MES 替代了 KIQ
- 负责 Queue 调度
- 从 MQD 加载 HQD，反之亦然

---

## 💡 对 POC Stage 1 的启示

### 1. MQD debugfs 的本质

```
/sys/kernel/debug/kfd/mqds
  │
  ├─ 显示所有 Queue 的 MQD 信息
  │   ├─ Queue ID (软件标识)
  │   ├─ Process PID
  │   ├─ GPU Virtual Address
  │   ├─ Priority
  │   └─ Active Status (软件视角)
  │
  └─ 这是软件层面的 Queue 状态
```

---

### 2. HQD debugfs 的本质

```
/sys/kernel/debug/kfd/hqds
  │
  ├─ 显示所有 HQD 的硬件寄存器
  │   ├─ CP Pipe 和 Queue 坐标
  │   ├─ HQD_ACTIVE 寄存器 (硬件视角)
  │   └─ 其他 55 个硬件寄存器
  │
  └─ 这是硬件层面的 Queue 状态
```

---

### 3. suspend_queues 的工作原理

基于官方文档，`suspend_queues` 应该：

```
1. 请求固件停止指定的 Queue
   ↓
2. 固件等待 HQD_ACTIVE = 0
   ↓
3. 固件保存 HQD → MQD
   ↓
4. Queue 被暂停
```

这正是 CWSR (Compute Wave Save/Restore) 的机制！

---

### 4. Queue ID 的本质

**软件层面 (MQD)**:
- Queue ID: 进程内的逻辑编号 (0, 1, 2, ...)
- 与 PID 关联
- 软件管理

**硬件层面 (HQD)**:
- 由 (Inst, Pipe, Queue) 三元组标识
- 例如: (0, 0, 5) 表示 Instance 0, Pipe 0, Queue 5
- 硬件坐标

**映射关系**:
```
MQD Queue ID (软件)  ←→  HQD (Inst, Pipe, Queue) (硬件)
                    ↑
                    由固件 (MES/KIQ) 管理
```

---

## 🔍 与我们实验的关联

### 为什么 MQD 文件有时为空？

**答案**: 因为当前没有活跃的 Queue！

从官方文档我们知道：
1. MQD 存储在**内存**中
2. 只有**映射的 Queue** 才会在 debugfs 中显示
3. Queue 结束后，MQD 被释放

**解决方案**: 必须在 GPU 程序**运行期间**查看 MQD！

---

### Queue 切换的时机

官方文档指出，Pipe 可以基于以下输入切换 Queue：

1. **Command Stream** (命令流)
2. **Packet by Packet** (逐包)
3. **Other hardware requests** (例如 MES 请求)

**对 POC 的意义**:
- 我们的 `suspend_queues` 属于第 3 类
- 通过 IOCTL → KFD → 固件 (MES) → HQD
- 固件负责实际的 Queue 切换

---

## 🎯 POC Stage 1 实施建议更新

### 基于官方文档的理解

#### 1. Queue ID 获取

```python
# MQD 提供软件层面的 Queue ID
def get_queue_ids_from_mqd(pid):
    """
    从 MQD debugfs 获取指定进程的 Queue IDs
    """
    queues = parse_mqd("/sys/kernel/debug/kfd/mqds")
    return [q.queue_id for q in queues if q.pid == pid]
```

**注意**: 
- Queue ID 是**进程内的逻辑编号**
- 与硬件 HQD 坐标不同
- 由 KFD 管理映射关系

---

#### 2. Queue 状态监控

```python
# 软件视角：MQD
mqd_active = check_mqd_active(queue_id)

# 硬件视角：HQD
hqd_active = check_hqd_active_bit(inst, pipe, queue)

# 两者可能不同！
if mqd_active and not hqd_active:
    print("Queue 软件认为活跃，但硬件未激活")
```

---

#### 3. suspend_queues 工作流程

```
用户程序 (POC Test Framework)
  ↓ IOCTL (KFD_IOC_DBG_TRAP_SUSPEND_QUEUES)
KFD Driver
  ↓ 调用固件接口
MES/KIQ Firmware
  ↓ 等待 HQD_ACTIVE = 0
  ↓ 保存 HQD → MQD
Queue 暂停成功
```

**关键点**:
- 固件负责实际操作
- 固件等待硬件就绪
- 固件管理 MQD ↔ HQD 转换

---

## 📊 架构图：MQD/HQD 完整流程

```
┌────────────────────────────────────────────────────────────────┐
│                     用户空间 (User Space)                       │
│                                                                 │
│  ┌──────────────┐    Doorbell    ┌──────────────┐             │
│  │  GPU Program │ ══════════════> │ Ring Buffer  │             │
│  │  (PyTorch)   │    (MMIO写)     │  (Commands)  │             │
│  └──────────────┘                 └──────────────┘             │
└────────────────────────────────────────────────────────────────┘
         │                                   │
         │ (1) 创建 Queue                    │ (2) 提交命令
         ▼                                   ▼
┌────────────────────────────────────────────────────────────────┐
│                    内核空间 (Kernel Space)                      │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    KFD (Kernel Fusion Driver)            │  │
│  │                                                          │  │
│  │  ┌─────────────┐         ┌─────────────┐               │  │
│  │  │   MQD       │ ◄─────► │   Debugfs   │               │  │
│  │  │  (Memory)   │         │  /kfd/mqds  │               │  │
│  │  │             │         └─────────────┘               │  │
│  │  │ Queue State │                                        │  │
│  │  │   - Queue ID│                                        │  │
│  │  │   - PID     │                                        │  │
│  │  │   - Address │         ┌─────────────┐               │  │
│  │  │   - Doorbell│ ◄─────► │   Debugfs   │               │  │
│  │  │   - HQD Regs│         │  /kfd/hqds  │               │  │
│  │  └─────────────┘         └─────────────┘               │  │
│  │         ▲                                               │  │
│  │         │ (3) MQD ↔ HQD                                │  │
│  │         ▼                                               │  │
│  │  ┌─────────────────────────────────────────────────┐   │  │
│  │  │            MES/KIQ Firmware                     │   │  │
│  │  │  (调度固件，管理 Queue 切换)                     │   │  │
│  │  │                                                 │   │  │
│  │  │  - Load MQD → HQD (激活 Queue)                  │   │  │
│  │  │  - Wait HQD_ACTIVE = 0                         │   │  │
│  │  │  - Save HQD → MQD (停用 Queue)                  │   │  │
│  │  └─────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
         │
         │ (4) 硬件操作
         ▼
┌────────────────────────────────────────────────────────────────┐
│                     硬件层 (Hardware)                           │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              GFX/Compute Engine                          │  │
│  │                                                          │  │
│  │  ┌─────────────────────────┐                            │  │
│  │  │        Pipe 0            │                            │  │
│  │  │  ┌───────────────────┐  │                            │  │
│  │  │  │  HQD 0            │  │  ← HQD_ACTIVE 寄存器       │  │
│  │  │  │  (硬件寄存器)      │  │                            │  │
│  │  │  └───────────────────┘  │                            │  │
│  │  │  ┌───────────────────┐  │                            │  │
│  │  │  │  HQD 1            │  │  ← 一次只执行一个 Queue    │  │
│  │  │  └───────────────────┘  │                            │  │
│  │  └─────────────────────────┘                            │  │
│  │                                                          │  │
│  │  ┌─────────────────────────┐                            │  │
│  │  │        Pipe 1            │                            │  │
│  │  │  ┌───────────────────┐  │                            │  │
│  │  │  │  HQD 0            │  │                            │  │
│  │  │  └───────────────────┘  │                            │  │
│  │  │  ┌───────────────────┐  │                            │  │
│  │  │  │  HQD 1            │  │                            │  │
│  │  │  └───────────────────┘  │                            │  │
│  │  └─────────────────────────┘                            │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔗 关键术语对照表

| 术语 | 英文 | 层次 | 说明 |
|------|------|------|------|
| **MQD** | Memory Queue Descriptor | 软件/内存 | Queue 状态在内存中的表示 |
| **HQD** | Hardware Queue Descriptor | 硬件 | Queue 在硬件寄存器中的表示 |
| **Pipe** | Pipe | 硬件 | 执行 Queue 的硬件线程 |
| **Queue** | Queue | 逻辑 | 命令队列 |
| **KIQ** | Kernel Interface Queue | 软件 | 内核控制队列 (旧) |
| **MES** | Micro-Engine Scheduler | 固件 | 微引擎调度器 (新，GFX 11+) |
| **CWSR** | Compute Wave Save/Restore | 机制 | 计算波保存/恢复 |
| **HQD_ACTIVE** | HQD_ACTIVE bit | 硬件 | 指示 Queue 是否活跃的硬件位 |

---

## 📝 重要发现总结

### 1. MQD 和 HQD 的本质区别

| 特性 | MQD | HQD |
|------|-----|-----|
| **位置** | 内存中 | 硬件寄存器中 |
| **管理者** | KFD + 固件 | 固件 + 硬件 |
| **可见性** | debugfs: `/kfd/mqds` | debugfs: `/kfd/hqds` |
| **标识** | Queue ID (进程内) | (Inst, Pipe, Queue) |
| **作用** | 保存 Queue 状态 | 控制硬件执行 |

---

### 2. Queue 生命周期

```
创建 → 映射 → 激活 → 执行 → 暂停 → 恢复 → 停用 → 销毁
  ↓      ↓      ↓      ↓      ↓      ↓      ↓      ↓
 MQD   固件   HQD   Pipe   固件   固件   固件   释放
       分配   加载   执行   保存   恢复   卸载   MQD
```

---

### 3. 抢占机制的正确理解

**基于官方文档**:

```
抢占请求 (suspend_queues)
  ↓
固件处理
  ↓
等待 HQD_ACTIVE = 0 (硬件空闲)
  ↓
保存 HQD → MQD (上下文保存)
  ↓
Queue 暂停
```

**关键**:
- ✅ 固件等待硬件空闲
- ✅ 自动保存上下文
- ✅ 硬件支持的机制

---

## 🎯 对 POC 测试的影响

### 更新的测试策略

#### 1. 正确的 Queue ID 获取

```bash
# 在程序运行期间查看
while [ 程序在运行 ]; do
    sudo cat /sys/kernel/debug/kfd/mqds | grep "Queue ID"
done
```

#### 2. MQD vs HQD 状态验证

```bash
# 软件视角 (MQD)
sudo cat /sys/kernel/debug/kfd/mqds | grep "is active"

# 硬件视角 (HQD)
sudo cat /sys/kernel/debug/kfd/hqds | awk 'NR % 58 == 3 { print $2 }'
```

#### 3. 理解 suspend_queues 的延迟

**预期行为**:
- 固件需要等待 HQD_ACTIVE = 0
- 如果 Queue 正在执行，会有延迟
- Grace period 参数控制等待时间

---

## 🔬 实验验证计划

### 实验 1: MQD 生命周期观察

```bash
# 1. 查看初始 MQD (应该为空)
sudo cat /sys/kernel/debug/kfd/mqds

# 2. 启动 GPU 程序
python gpu_test.py &

# 3. 观察 MQD 创建
sudo cat /sys/kernel/debug/kfd/mqds

# 4. 等待程序结束

# 5. 观察 MQD 销毁 (应该再次为空)
sudo cat /sys/kernel/debug/kfd/mqds
```

---

### 实验 2: HQD_ACTIVE 位监控

```bash
# 持续监控 HQD_ACTIVE 位
watch -n 1 'sudo cat /sys/kernel/debug/kfd/hqds | awk "NR % 58 == 3 { print \$2 }"'
```

---

### 实验 3: suspend_queues 延迟测试

```python
# 测试 grace period 对延迟的影响
for grace in [100, 1000, 10000]:  # us
    start = time.time()
    suspend_queues([qid], grace)
    latency = time.time() - start
    print(f"Grace {grace}us → Latency {latency*1000}ms")
```

---

## 📚 参考资料

1. **官方文档**: https://docs.kernel.org/gpu/amdgpu/driver-core.html
2. **关键章节**: "GFX, Compute, and SDMA Overall Behavior"
3. **相关文档**:
   - `/mnt/md0/zhehan/code/coderampup/private_github/amdgpudriver/doc/scheduler/DOC_POC_stage1/ARCH_Design_04_HQD信息获取完整指南.md`
   - `/mnt/md0/zhehan/code/coderampup/private_github/amdgpudriver/doc/kernel_submit/test_queue_limits/logs_KCQ_config/PRECISE_HQD_COUNTING_METHOD.md`

---

## ✅ 关键结论

### 1. MQD 和 HQD 是 Queue 在不同层次的表示

- **MQD**: 软件/内存层，存储 Queue 状态
- **HQD**: 硬件层，控制实际执行

### 2. 固件 (MES/KIQ) 是关键中介

- 管理 MQD ↔ HQD 转换
- 负责 Queue 调度和切换
- 等待硬件就绪后操作

### 3. HQD_ACTIVE 是核心状态位

- bit[0] = 1: Queue 活跃
- bit[0] = 0: Queue 空闲，可安全操作
- 固件在切换前等待此位

### 4. POC Stage 1 的 suspend_queues 是正确的方向

- 利用官方支持的机制
- 固件自动处理上下文保存
- 硬件级别的 Queue 切换

---

**最后更新**: 2026-02-03  
**基于**: Linux Kernel 6.19.0-rc8 Documentation

这份官方文档完美地验证了我们之前的研究方向！✅
